1. 기본키 외래키 설정

ALTER TABLE USER_INFO ADD PRIMARY KEY(U_NO);
ALTER TABLE SUB_BOARD ADD PRIMARY KEY(SB_NO);
ALTER TABLE RELATION ADD PRIMARY KEY(R_NO);
ALTER TABLE MBTI ADD PRIMARY KEY(M_NO);
ALTER TABLE CHAT ADD PRIMARY KEY(C_NO);
ALTER TABLE BOARD ADD PRIMARY KEY(B_NO);

ALTER TABLE BOARD
ADD CONSTRAINT fk_b
FOREIGN KEY(U_NO)
REFERENCES USER_INFO(U_NO);

ALTER TABLE SUB_BOARD
ADD CONSTRAINT fk_sb_u
FOREIGN KEY(U_NO)
REFERENCES USER_INFO(U_NO);

ALTER TABLE SUB_BOARD
ADD CONSTRAINT fk_sb_b
FOREIGN KEY(B_NO)
REFERENCES BOARD(B_NO);

2. 데이터 제약조건

U_ID ( 인터넷 사이트 참고 )
U_PW ( 참고 )
U_NAME 1BYTE이상
U_GENDER 남/여
U_AGE 20~99
U_LOC  1BYTE 이상
U_HOBBY *
U_TALENT *
U_MBTI 16가지유형 예시) constraints ch_u_mbti check ( U_MBTI IN ('INTJ','ENTP'));
U_IMG *
U_LIKE default 0, 1이상 정수


B_TITLE 1BYTE 이상
B_CONTENT 1BYTE 이상
B_DATE *
B_IMG *
B_LIKE default 0, 1이상 정수
U_NO 1이상 정수

SB_CONTENT 1BYTE 이상
SB_DATE *
U_NO 1이상 정수
B_NO 1이상 정수
SB_LIKE default 0, 1이상 정수

R_NO  1이상 정수
R_TYPE1 16가지유형
R_TYPE2 16가지유형
R_LEVEL check (between 1 and 5);

C_FROM_ID 
C_TO_ID 
C_CONTENT 1BYTE 이상
C_DATE *
C_COMMENT 1BYTE 이상

M_NO 1이상 정수
M_TYPE 16가지유형
M_CONTENT 1BYTE 이상

3.  CLOB/ BLOB 데이터타입 변경
ALTER TABLE USER_INFO ADD (IMG BLOB);
ALTER TABLE USER_INFO DROP COLUMN U_IMG;
ALTER TABLE USER_INFO RENAME COLUMN IMG TO U_IMG;

4. 시퀀스 생성 U_NO / B_NO / B_HIT / SB_NO /C_NO 
CREATE SEQUENCE U_NO_SEQ 
INCREMENT BY 1 
START WITH 0 
MINVALUE 0 
MAXVALUE 1000 
NOCYCLE;

시퀀스 검색
SELECT SEQUENCE_NAME, MIN_VALUE, MAX_VALUE, INCREMENT_BY, CYCLE_FLAG 
FROM USER_SEQUENCES;


